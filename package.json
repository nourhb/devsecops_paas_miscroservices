{
  "name": "devsecops-paas-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix paas/frontend",
    "build": "npm run build --prefix paas/frontend",
    "db:migrate": "cd paas/frontend && npx prisma migrate deploy",
    "db:studio": "cd paas/frontend && npx prisma studio",
    "docker:db": "cd paas && docker compose up -d postgres",
    "docker:db:reset": "cd paas && docker compose down -v && docker compose up -d postgres"
  }
}
