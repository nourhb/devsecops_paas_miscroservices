# Lab VM browser access — http://192.168.56.129:30100/login
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: paas
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30100
