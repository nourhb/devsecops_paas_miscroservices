{
  "name": "devsecops-paas",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "db:generate": "npm run prisma:generate --prefix frontend",
    "db:push": "npm run prisma:push --prefix frontend",
    "db:studio": "npm run prisma:studio --prefix frontend",
    "db:push:generate": "npm run prisma:push:generate --prefix frontend"
  }
}
