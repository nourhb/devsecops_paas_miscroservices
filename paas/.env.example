# Optional Docker Compose override (paas/docker-compose.yml loads this if present).
# Copy secrets to frontend/docker-compose.env instead — that is the main env file.
# Never commit paas/.env (gitignored).
